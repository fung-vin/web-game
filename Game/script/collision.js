  var detectBullet = function (x, y, ,width, height, x2, y2, width2, height2) {

    var x = x;
    var y = y;
    var width = width;
    var height = height;

    var xTwo = x2;
    var yTwo = y2;
    var widthTwo = width2,
    var heightTwo = height2;

    element = $('<div class="enemy"></div>')[0];

    if (x < xTwo + widthTwo && x + width > xTwo && y < yTwo + heightTwo && height + y > yTwo) {
      $(element).remove();
    }

  }

  var detectBullet = function (x, y, ,width, height,) {

    var x = x;
    var y = y;
    var width = width;
    var height = height;

  }

  if (x < xTwo + widthTwo && x + width > xTwo && y < yTwo + heightTwo && height + y > yTwo) {
      $(element).remove();

  }


  bulletInfo

  enemyPosition


  bulletInfo.x < enemyPosition.left + enemyPosition.width &&
  bulletInfo.x < bulletInfo.width > enemyPosition.left &&
  bulletInfo.y < enemyPosition.top + enemyPosition.height &&
  bulletInfo.height + bulletInfo.y > enemyPosition.top

var rect1 = {x: 5, y: 5, width: 50, height: 50}
var rect2 = {x: 20, y: 10, width: 10, height: 10}

if (rect1.x < rect2.x + rect2.width &&
   rect1.x + rect1.width > rect2.x &&
   rect1.y < rect2.y + rect2.height &&
   rect1.height + rect1.y > rect2.y)

bulletInfo x, y, width, height
enemyPosition left, top, width height

bulletInfo.x < enemyPosition.left + enemyPosition.width &&
bulletInfo.x + bulletInfo.width > enemyPosition.left &&
bulletInfo.y < enemyPosition.top + enemyPosition.height &&
bulletInfo.y +bulletInfo.height > enemyPosition.top